# دليل التسليم — موقع بورتفوليو عبدالله ماهر

## نظرة عامة

موقع بورتفوليو شخصي ثابت باللغة العربية (RTL) بأسلوب Pentagram (شبكة سويسرية + أسود/أبيض + قرمزي). يحتوي 5 أقسام: بطل، نبذة، مشاريع، مهارات، تواصل.

---

## 1. تشغيل محلي

```bash
cd portfolio-site
python3 -m http.server 8788   # ← افتح http://localhost:8788
```

أو أي خادم ملفات ثابتة (Live Server، Vite preview، npx serve...).

---

## 2. تغيير المحتوى

### تغيير النبذة أو المهارات
- افتح `index.html` وعدّل النصوص مباشرة في الأقسام `#about` و `#skills`
- الملفات بترميز UTF-8 — استخدم محررًا يدعم العربية

### إضافة مشروع جديد
1. أضف صورة مربعة (800×800px) في `img/`
2. افتح `js/main.js` وأضف كائنًا جديدًا في مصفوفة `PROJECTS`:
```js
{
  img:    './img/اسم-الصورة.jpg',
  cat:    'الفئة',
  name:   'اسم المشروع',
  desc:   'وصف مختصر',
  link:   'https://رابط-المشروع',
  linkLabel: 'نص الزر'
}
```
3. البطاقة ستظهر تلقائيًا — لا حاجة لتعديل HTML

### تغيير لون التمييز
في `css/style.css` غيّر `--accent: #D93A2B` إلى أي لون تريده

### تغيير بريد التواصل
في `js/main.js` غيّر `abdalla2.1993@gmail.com` في رابط FormSubmit

---

## 3. النشر على GitHub Pages

### المتطلبات
- حساب GitHub مع repo يحوي ملفات الموقع
- مفتاح SSH أو token بصلاحية كتابة

### خطوات النشر
```bash
# 1. أنشئ repo (إن لم يوجد)
#    اذهب إلى github.com/new → اسم: portfolio → Create repository

# 2. ادفع الكود
cd portfolio-site
git init -b main
git add .
git commit -m "بورتفوليو: النسخة الأولى"
git remote add origin git@github.com:abdallamaher1993/portfolio.git
git push -u origin main

# 3. فعّل GitHub Pages
#    اذهب إلى Settings → Pages → Source: main / (root) → Save
#    الرابط: https://abdallamaher1993.github.io/portfolio/
```

### النشر بعد تعديل
```bash
git add -A && git commit -m "تحديث" && git push
```

---

## 4. التراجع

### نسخة احتياطية محلية
الملف `portfolio-site-v1.zip` يحوي كل شيء (باستثناء workflows). للاسترجاع:
```bash
unzip -o portfolio-site-v1.zip
```

### تراجع عبر Git
```bash
git log --oneline -5          # عثر على التعيين السليم
git revert HEAD               # تراجع عن آخر تعديل
# أو:
git reset --hard <hash> && git push --force   # ⚠ خطر — يستبدل التاريخ
```

---

## 5. استكشاف الأخطاء

| المشكلة | الحل |
|---|---|
| الصفحة بيضاء | تأكد من أن `index.html` و `css/style.css` و `js/main.js` في نفس المجلد |
| الخطوط لا تظهر | تحقق من الاتصال بالإنترنت (Google Fonts CDN) — أو استبدل بخطوط محلية |
| نموذج التواصل لا ينجح | أول إرسال يطلق بريد تفعيل إلى abdalla2.1993@gmail.com — افحص صندوق البريد (بما فيه الرسائل غير المرغوبة) |
| الصور لا تظهر | تأكد من وجودها في `img/` وأن الأسماء تطابق ما في `main.js` |
| Pages يعرض 404 | تأكد من تفعيل Pages في Settings → Pages → Source: main / root |

---

## 6. ملاحظات

- **FormSubmit.co**: خدمة مجانية بدون تسجيل. أول إرسال يطلق بريد تفعيل. بعد التفعيل، كل رسالة تصل إلى البريد بسجل كامل.
- **الصور**: 8 صور مأخوذة من مكتبة فيديوهات المستخدم (أعماله المملوكة) — قابلة للاستبدال بأي صور مربعة.
- **الخطوط**: Amiri (عناوين) + IBM Plex Sans Arabic (جسم) + IBM Plex Mono (أرقام) — من Google Fonts.
- **الأسلوب**: Pentagram — شبكة 12 عمود، خطوط 1px سوداء، لون مميز واحد (قرمزي)، طباعة ضخمة.
